# Chorus — priced per person

Three tiers, where the honest figure is what each person pays. It's the big
number on every card; the plan's own price is the line underneath.

| # | File | Screen | `-pShot` |
|---|---|---|---|
| 01 | `01-solo.png` | The three tiers, solo selected | `chorus` |
| 02 | `02-family.png` | — family selected, and the fine print with it | `chorusFamily` |

The "least each" badge is computed from the prices, so it moves on its own if
one of them changes.

Composites in `images/`. See `../README.md` for how these are captured.
