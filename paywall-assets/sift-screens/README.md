# Sift — the allowance ran out

The paywall nobody opens. It arrives mid-task, so it shows what you used, says
when it comes back, and leaves the inbox it interrupted visible behind it.

| # | File | Screen | `-pShot` |
|---|---|---|---|
| 01 | `01-limit.png` | Five of five used, one price and a link | `sift` |
| 02 | `02-plans.png` | The plans, unfolded | `siftPlans` |

The inbox behind the sheet is a real view in the app, not a screenshot — the
sheet is only honest if the thing it interrupted is still there when you
decline it.

Composites in `images/`. See `../README.md` for how these are captured.
